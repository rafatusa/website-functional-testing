# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: destroy
'on':
  workflow_dispatch: {}
jobs:
  destroy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: No infrastructure to destroy
      run: echo "website-functional-testing provisions no cloud resources; nothing
        to tear down."
